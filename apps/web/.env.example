# Browser + same-origin proxy (Next rewrites /api → API_PROXY_TARGET)
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Server-side rewrite target
# Local/tunnel: http://127.0.0.1:4101
# Docker web service: http://api:3000
API_PROXY_TARGET=http://127.0.0.1:4101
